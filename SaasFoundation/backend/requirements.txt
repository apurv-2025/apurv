# ###########################################################################################################
# backend/requirements.txt
#
# This file contains the dependencies for the application.
# It uses the fastapi library to create the application.
# It uses the sqlalchemy library to create the database engine and session factory.
# It uses the os library to get the database URL from the environment variables.
# ###########################################################################################################

fastapi==0.104.1
uvicorn==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0
python-decouple==3.8
email-validator==2.1.0
httpx==0.25.2
